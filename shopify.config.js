import CustomConfig from './custom.config';

export default {
  locale: 'en-us',
  storeDomain: CustomConfig.storeDomain,
  storefrontToken: CustomConfig.storefrontToken,
  graphqlApiVersion: 'unstable',
};
